# ASI-Arch ThoughtSeed System Requirements
# Core ThoughtSeed dependencies - minimal ML footprint

# Core dependencies
numpy>=2.0.0,<3.0.0
scipy>=1.11.0
redis>=5.0.0
asyncio-mqtt>=0.16.0

# Essential ML (skip torch/transformers for now - use minimal setup)
# torch>=2.2.0,<2.3.0  # Current available version
# torchvision>=0.17.0,<0.18.0

# Database and storage
neo4j>=5.0.0
aiofiles>=23.0.0
aiosqlite>=0.19.0

# Web and API
fastapi>=0.100.0
uvicorn>=0.23.0
aiohttp>=3.8.0
httpx>=0.25.0

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.12.0

# Utilities
pydantic>=2.0.0
python-dotenv>=1.0.0
typer>=0.9.0
rich>=13.0.0
tqdm>=4.65.0

# Optional ML dependencies (install manually if needed)
# tensorflow  # Skip for now due to NumPy compatibility
# atlas-rag   # Skip for now due to NumPy compatibility